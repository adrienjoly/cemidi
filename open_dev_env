# make sure to switch to the script's dir (e.g. when launched via mac osx's finder)
cd `dirname "$0"`

open mongod

# sleep 2
# open dbserver
# sleep 2
# curl --data server=localhost:27017 'http://localhost:27080/_connect'
# curl --data 'docs=[{"x":1}]' 'http://localhost:27080/default/restaurant/_upsert'

./run_local
